name: xesn
channels:
    - conda-forge
dependencies:
# Basics
    - numpy
    - scipy
    - matplotlib
# xarray et al
    - xarray
    - netCDF4
    - bottleneck
    - dask[complete]
    - zarr
# surrogate modeling toolbox
    - smt>=2.2.1,<2.8.0
# jupyter etc
    - jupyter
# Testing
    - pytest
    - coverage
# GPUs
#    - cupy
#    - cupy-xarray
#    - dask_cuda
