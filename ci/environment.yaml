name: xesn
channels:
    - conda-forge
dependencies:
# Basics
    - numpy
    - scipy
      #- cupy
    - matplotlib
# xarray et al
    - xarray
    - netCDF4
    - bottleneck
    - dask[complete]
    - zarr
# ML and optim
    - smt
# jupyter etc
    - jupyter
# Testing
    - pytest
    - coverage
